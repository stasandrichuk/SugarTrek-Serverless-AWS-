'use strict';

// Your first function handler
module.exports.hello = (event, context, cb) => {
  // throw "Error2";
  cb("[404]", {"statusCode": "[404]", "headers": {'Cache-Control': "max-age=120"}, body: JSON.stringify({message: 'Go Serverless2 v1.0! Your function executed successfully!', event })});
};

// You can add more handlers here, and reference them in serverless.yml
